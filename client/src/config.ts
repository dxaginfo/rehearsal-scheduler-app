// API Configuration
export const API_URL = process.env.REACT_APP_API_URL || '/api';

// App Configuration
export const APP_NAME = 'Rehearsal Scheduler';

// Feature flags
export const FEATURES = {
  SMART_SCHEDULING: true,
  CALENDAR_SYNC: true,
  NOTIFICATIONS: true,
};
